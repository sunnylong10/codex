# MNC Job Scraper v8 — dependencies
# install with:  pip install -r requirements.txt
# then run:      playwright install chromium

requests>=2.31.0          # ATS APIs (scraper.py + discover_ats.py)
pandas>=2.0.0             # data wrangling + Excel export
openpyxl>=3.1.0           # xlsx writing/styling
playwright>=1.40.0        # headless-browser fallback crawler
beautifulsoup4>=4.12.0    # HTML parsing helpers
lxml>=4.9.0               # fast parser backend for bs4
